# NER Converter — 変更履歴

バージョンは連番。更新のたびに 1 つ増やす。
旧 ZIP の `v1` … `v7` の続きとして **v8 から開始**している。
`app.js` の `APP_VERSION`、配布 ZIP のファイル名、この履歴の 3 つを必ず一致させること。
公開中のサイトのバージョンはページ下部のフッターに表示される。

---

## v8 — 2026-09-21

日本語版のセーブ構造を ROM の実バイトから確定させ、金・銀の不具合を修正した。

### 修正

- **金・銀: 副パディング `0x7E8C`–`0x7F0C` をクリアするようにした。**
  従来は主側 (`0x2C8C`–`0x2D0C`) しかクリアしておらず、副セーブ側に実機由来の
  ゴミ値 129 バイトがそのまま残っていた。後述のとおり金・銀はこの領域に
  余裕が無く、1 バイトでも残るとポケムーバーに認識されない。
- **金・銀: VC フッター 16 バイトを付加するようにした。**
  正常動作する日本語版シルバーの VC セーブから採取した実測値
  (`33 01 00 00 00 00 00 00 41 AB 5A AA 1F 00 00 00`)。従来は未設定だった。
- **金・銀: 入力の先頭 `0x8000` バイトだけを使うようにした。**
  従来は `0x8010` バイトまでコピーしており、`0x8000` より大きい入力では
  元ファイルのバイトが末尾 16 バイト (フッター位置) へ流れ込んでいた。

### 変更なし(検証の結果、正しいことが確認された)

- クリスタルのクリア範囲 `0x2AE3`–`0x2D0C` / `0x7CE3`–`0x7F0C`
- 金・銀のチェックサム範囲 `0x2009`–`0x2C8B` / `0x7209`–`0x7E8B`

### 根拠

- ROM のセーブ転送ルーチンから、ゲームデータ長を確定。
  クリスタル `ld bc,$0ADA` = 2778 バイト、金・銀 `ld bc,$0C83` = 3203 バイト。
- ポケムーバーの検証範囲を実機テスト 7 件で特定。
  検証終端 = チェックサム格納位置 − 国際版のギャップ − 1。
  クリスタルは `0x2B82`、金・銀は `0x2D0C`。
- 詳細は `analysis/poke_transporter_analysis.md` と
  `analysis/crystal_jp_save_layout.md`(リポジトリ外の解析資料)。

### 実機検証

- クリスタル: VC 起動・ロード / ゲーム内セーブ / ポケムーバー認識 / 育て屋・タマゴ
- 銀: VC 起動・ロード / ポケムーバー認識
- 金: 未検証(フッターは銀から採取したものを流用)

### 補足: 副パディングのクリアについて

実機テストの結果、**ポケムーバーは主セーブ (`0x2009`–`0x2D0C`) しか検証していない**ことが
判明した。副セーブの検証値が壊れていてもポケムーバーは通る。

したがって副パディングのクリアはポケムーバーのために必須ではない。
ただし副セーブはゲーム本体がロードに使う可能性があり、
本物の VC セーブでは全て `00` であるため、引き続きクリアする。

---

## v7 以前

`v1` … `v7_CrystalFix` という名前の ZIP が存在する。

**`v7_CrystalFix` は使用しないこと。** クリスタルのクリア範囲を `0x2B83`–`0x2D0C` に
狭めたものだが、これは誤りで、ポケムーバーに認識されなくなる。
ポケムーバーが実際に見ているのは `0x2AE3`–`0x2B82` の側である。

公開サイトで動いていたコードは v1 相当だった。v8 はそこからの更新である。
